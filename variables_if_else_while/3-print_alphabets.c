#include <stdio.h>
/**
 * main - alphabet soups
 *
 * Return:(0)
 */
int main(void)
{
	for (char i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	for (char i = 'A'; i <= 'Z'; i++)
	{
		putchar(i);
	}
	return (0);
}
